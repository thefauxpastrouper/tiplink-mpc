# Frontend Implementation
- Create a New Folder `frontend` which will contain a basic frontend for testing the backend created.
- Implement the proper mpc now that i have the client. Explain where should the client's share should be generated adn how it should be stored.
